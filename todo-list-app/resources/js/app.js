//modules needed
import { createApp } from 'vue';
import App from './components/App.vue';

// Create a new Vue application and mount it to the element with id 'app'
const app = createApp(App);
app.mount('#app');
